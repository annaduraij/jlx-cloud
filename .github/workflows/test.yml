name: Test

on:
  push:
    branches:
      - main
  pull_request:

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - name: Install validators
        run: |
          sudo apt-get update
          sudo apt-get install -y cloud-init shellcheck
      - name: Run checks
        env:
          REQUIRE_CLOUD_INIT: "1"
          REQUIRE_SHELLCHECK: "1"
        run: ./tests/test.sh
